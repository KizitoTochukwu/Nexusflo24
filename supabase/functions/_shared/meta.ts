// Embedded Signup with config_id no longer sends a redirect_uri.
// Kept as an empty export for backwards compatibility with older imports.
export const META_REDIRECT_URI = "";

// Default Meta Embedded Signup configuration for NexusFlo24.
// Overridable via the META_EMBEDDED_SIGNUP_CONFIG_ID secret on the backend.
export const DEFAULT_META_EMBEDDED_SIGNUP_CONFIG_ID = "945252895216570";
