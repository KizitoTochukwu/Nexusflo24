

## Expand Embed Code Dialog

Increase the dialog width from `sm:max-w-2xl` (672px) to `sm:max-w-3xl` (768px) to give the content more room and reduce vertical scrolling.

### Change

**File: `src/components/funnels/EmbedCodeDialog.tsx`** (line ~97)

- Change `sm:max-w-2xl` to `sm:max-w-3xl` on the `DialogContent` className.

