The NexusFlo24 MCP server is fully authored and configured, but the `mcp` Edge Function is not currently deployed (live endpoint returns `NOT_FOUND` and no function logs exist). This plan deploys it and verifies it end-to-end.

## Current state (verified)
- `src/lib/mcp/index.ts` defines an OAuth-secured MCP server with 4 tools.
- `supabase/functions/mcp/index.ts` is auto-generated by the Vite plugin.
- `.lovable/mcp/manifest.json` is present and current.
- OAuth consent page is wired at `/.lovable/oauth/consent`.
- Supabase OAuth server is enabled with dynamic client registration.
- Live probe of `https://stuaikfyuwcjmchcvfie.supabase.co/functions/v1/mcp/.well-known/oauth-protected-resource` returns `404 NOT_FOUND`, and no `mcp` edge-function logs exist → the function is not deployed.

## Steps

1. **Deploy the `mcp` Edge Function**
   - Use `supabase--deploy_edge_functions` with `["mcp"]`.
   - This is the only state-changing step; it pushes the generated function source to Lovable Cloud / Supabase Edge Functions.

2. **Verify the live endpoint exists**
   - `GET /functions/v1/mcp/.well-known/oauth-protected-resource` should return `200` with OAuth resource metadata.
   - If it still 404s, inspect the deploy output and edge function logs.

3. **Verify tool discovery**
   - Call `POST /functions/v1/mcp/.mcp/list-tools` with the required MCP Streamable HTTP headers (`Accept: application/json, text/event-stream`, `Content-Type: application/json`).
   - Confirm it returns the 4 advertised tools matching the manifest.

4. **Verify OAuth flow wiring**
   - Confirm the consent page at `https://nexusflo24.com/.lovable/oauth/consent` loads and handles `authorization_id`.
   - Check that the redirect allow-list includes the published and preview domains (already configured).

5. **Update the MCP manifest (if needed)**
   - Re-run `app_mcp_server--extract_mcp_manifest` after deployment to ensure the manifest reflects the deployed state.

## Deliverable
A deployed, reachable NexusFlo24 MCP server at `https://stuaikfyuwcjmchcvfie.supabase.co/functions/v1/mcp` with working OAuth and tool discovery.