

## Plan: Update WHATSAPP_ACCESS_TOKEN

Replace the existing `WHATSAPP_ACCESS_TOKEN` secret with the new permanent token provided.

### Step
1. Use `add_secret` to overwrite `WHATSAPP_ACCESS_TOKEN` with the new value.

No code changes required.

